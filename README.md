# hexagonal-library-system
A CLI library lending system in Java demonstrating hexagonal (ports &amp; adapters) architecture, separates domain logic from driving/driven adapters, built with Gradle and tested with JUnit 5.
